const form=document.getElementById('request-form');const description=document.getElementById('description');const counter=document.getElementById('char-count');const status=document.getElementById('form-status');description.addEventListener('input',()=>counter.textContent=description.value.length);
form.addEventListener('submit',async event=>{event.preventDefault();status.className='form-status';status.textContent='';if(!form.reportValidity())return;const button=form.querySelector('button[type="submit"]');button.disabled=true;button.textContent='Enviando…';const data=Object.fromEntries(new FormData(form));data.consent=document.getElementById('consent').checked;try{const response=await fetch('https://black-knights-private-software.evan2003gustavo.chatgpt.site/api/requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const result=await response.json().catch(()=>({}));if(!response.ok)throw new Error(result.message||'No pudimos enviar la solicitud.');form.reset();counter.textContent='0';status.className='form-status success';status.textContent=`Solicitud ${result.reference} recibida. Te contactaremos después de revisarla.`;button.textContent='Solicitud enviada';}catch(error){status.className='form-status error';status.textContent=error.message;button.disabled=false;button.textContent='Enviar solicitud';}});
